function translateShadow(x, y, blur, color) {
  // Translate the shadow horizontally by 'x' pixels
  const translatedShadow = `box-shadow: ${x}px ${y}px ${blur}px ${color};`;

  // Rest of the code...
}
